{% block prompt %}
  <p>Let's work this out in a step by step way to ensure we have the right answer.</p>

  ```python
  # This is a simple for loop that iterates through a list of numbers
  # and prints each number to the console.
  numbers = [1, 2, 3, 4, 5]
  for number in numbers:
      print(number)
  ```
{% endblock %}
